{% extends 'VilkaCoreBundle::layout.html.twig' %}
{% set is_book = true %}
{% block title %}Создание пользователя {{ app.session.get('settings').separator }} {{ parent() }}{% endblock %}

{% block content %}

    <div class="row">
        <div class="col-lg-12">
            <div class="row">
                <div class="col-lg-12">
                    <div class="main-box">
                        <h2>Создание пользователя</h2>
                        {{ form_start(form) }}
                        {{ form_errors(form) }}
                        <div class="form-group">
                            {{ form_label(form.username, 'Имя пользователя') }}
                            {{ form_widget(form.username, { 'attr': {'class': 'form-control', 'placeholder': 'Введите имя пользователя'} }) }}
                            {{ form_errors(form.username) }}
                        </div>
                        <div class="form-group">
                            {{ form_label(form.email, 'Email') }}
                            {{ form_widget(form.email, { 'attr': {'class': 'form-control', 'placeholder': 'Введите email'} }) }}
                            {{ form_errors(form.email) }}
                        </div>
                        <div class="form-group">
                            {{ form_label(form.password, 'Пароль') }}
                            {{ form_widget(form.password, { 'attr': {'class': 'form-control', 'placeholder': 'Введите пароль'} }) }}
                            {{ form_errors(form.password) }}
                        </div>
                        <div class="form-group">
                            {{ form_label(form.password_confirm, 'Подтверждение пароля') }}
                            {{ form_widget(form.password_confirm, { 'attr': {'class': 'form-control', 'placeholder': 'Повторите пароль'} }) }}
                            {{ form_errors(form.password_confirm) }}
                        </div>
                        <div class="form-group">
                            {{ form_label(form.enabled, 'Активен') }}
                            {{ form_widget(form.enabled, { 'attr': {'class': 'form-control enabled'} }) }}
                        </div>
                        <div class="form-group">
                            {{ form_widget(form.submit, { 'attr': {'class': 'btn btn-success'} }) }}
                        </div>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
